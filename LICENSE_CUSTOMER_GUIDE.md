# 栖缘社 License 交付说明（客户侧）

本文面向私有化部署的客户，说明 License 授权相关的环境变量配置与激活方式。

## 一、环境变量配置

在项目根目录 `.env` 中配置以下三项：

```env
# 许可证服务器地址（经授权方主域名反代，隐藏真实源站）
LICENSE_SERVER_URL=https://date.arvine.cn/license/api

# 客户端接口鉴权密钥（由授权方随部署包提供，与许可证服务器一致）
LICENSE_SERVER_SECRET=<授权方提供>

# 本实例部署域名（用于授权台账展示）
APP_DOMAIN=你的域名
```

> 若 `LICENSE_SERVER_URL` 发生变更（授权方更换域名/IP），授权方会通知更新此值；未及时更新时，激活会自动降级为离线激活，不影响已激活实例运行。

## 二、激活 License

1. 登录管理后台 → 「系统授权」页面。
2. 粘贴授权方签发的 License Key。
3. 点击激活。

激活成功即写入本地记录，系统进入 `valid` 状态。

## 三、离线激活兜底

- 若激活时许可证服务器不可达（网络故障/维护），后端会**自动降级为离线激活**：本地验签通过即生效，系统按 License 过期时间正常运行。
- 离线激活**不受在线激活次数限制**，后续每天心跳在线时会自动对账补齐激活记录。
- 已激活实例在许可证服务器不可达期间照常运行（心跳失败仅记录日志，不影响业务）。

## 四、注意事项

1. `LICENSE_SERVER_SECRET` 请妥善保管，不要提交到公开仓库或泄露给无关人员。
2. 管理后台「系统授权」页面仅在管理网使用，不要暴露到公网。
3. 到期与远程吊销依赖在线心跳；长期离线将无法接收远程吊销指令。
